/**
 * Stub — the real @buildpass/db package is being built by another agent.
 * This placeholder allows workspace resolution for dependent packages.
 */
export type DbClient = Record<string, unknown>;
