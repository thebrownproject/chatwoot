# @chatwoot-next/sdk-shared

Shared TypeScript types between the embeddables (widget, sdk, survey) and `apps/api-public`. No runtime code — just discriminated unions for realtime events, public-API entity shapes, and widget runtime config — so both sides of the public API stay in lockstep as the embeddables migrate off the legacy Vue widget.
