# Voice channel (Enterprise)

Enterprise-only Twilio Voice channel. The stub here exists so the registry doesn't have to branch on edition; the real implementation will be wired in `apps/enterprise`.
