# @chatwoot-next/widget

Customer-facing chat bubble. Built with Preact (not React) to fit the 200KB gzipped bundle budget required for an embedded cross-domain widget injected via the SDK iframe loader. Replaces `app/javascript/widget/` from the legacy app.
